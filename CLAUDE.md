# CLAUDE.md — 飲食店向け原価計算・メニュー値付けツール

> **別PC・新セッションで始めるときは、まず [docs/HANDOFF.md](docs/HANDOFF.md) を読むこと**(現状・決定事項・次のTODO・手順がまとまっている)。
> サービス名は「ゲンカル」(旧称: 原価計算ツール)。LPは `landing/`(別のVercelプロジェクト)。

## 作業者プロフィール(最重要・毎回意識すること)

- 開発者(Mahiro)はvibeコーディングが主。自力でコードを書く/読んで正誤を判断する力はほぼない
- 本業はSE(直近は上流工程中心)、実装力ではなく企画・判断力で仕事をしている
- **コードの正しさは「読んで判断」ではなく「動作結果・テスト結果・検算」で判断する**という前提で進めること
- 変更は小さい単位で提案し、各ステップで「何をしたか・なぜか」を平易な日本語で説明すること
- 複雑な実装を一気に出さず、途中で止めて動作確認を挟むこと

## プロダクトの位置づけ

- 個人経営の飲食店向け、原価計算・メニュー値付けの「業務組み込み型ツール」(毎日/毎週使われる前提。ワンショットの確認ツールにしない)
- 差別化軸: AIベースのCSV/Excel自動マッピング取り込み、食材価格変動時の自動再計算、メニュー収益性ランキング+値上げアラート
- スコープ外(絶対に手を広げない): メニュー提案などのクリエイティブ機能、在庫管理、POS連携(実装済み機能除く)、デリバリー機能
- ターゲットは個人店(単店)。多店舗/FC向けの機能は作らない(競合のSC Consultingはそちら側なので差別化ポイントとして意図的に避けている)

## 絶対に守るルール

- データ取得は **www.maff.go.jp 配下の「旬別結果」CSV/PDFのみ使用可**。
  `seisen.maff.go.jp` は robots.txt disallow のため絶対にスクレイピング/fetchしない(検討済み・再提案不要)
- 数値・データのみを扱うツールとして設計する。メニュー提案等のクリエイティブ機能は提案されても採用しない
- スマレジPOS連携はpost-MVPロードマップ項目。今のスコープに含めない

## ドメイン用語

- 原価率 = 食材原価 ÷ 売価。メニュー単位の値付け判断に使う指標。メニューごとに個別の目標原価率を設定でき(メニュー編集画面、任意項目)、未設定なら店舗全体の目標原価率(店舗設定)にフォールバックする。値付け戦略上、意図的に原価率が高いメニュー(海鮮系など)を個別に許容したい場合に使う
- 理論原価率 = レシピ通りに計算した原価率(=F比率と同じ)。実質原価率 = 理論原価率 +(月次のロス・値引き額 ÷ 売上)× 100 という単純加算の概算値。ロス・値引き額はstore_fixed_costsに家賃・人件費と同じ扱いで月次入力する(cost_type='loss')。正常/注意/危険の色分けはFL/FLR比率専用で、原価率(理論・実質とも)には付けない(業界目安が存在しないため)
- FL比率 = (Food cost + Labor cost) / Sales。店舗全体の経営健全性を見る指標、目安は60%以下が健全ライン
- FLR比率 = (Food cost + Labor cost + Rent) / Sales。家賃まで含めた拡張版、目安は70%未満が理想。家賃(rent)はstore_fixed_costsテーブルで管理し、店舗設定画面(/settings)で入力する。原価率とFL/FLR比率は別の指標であり、混同しないこと
- 「確認ツール」(購入前の軽いチェック、低い支払意思)ではなく「業務組み込み型ツール」(継続利用、支払意思が根付く)として作ること

## 技術構成(現状)

- Next.js / TypeScript / Supabase / Vercel
- 実装済み: CSV/Excel AIマッピング取り込み(結合セルのフォワードフィル対応、テスト34本)、野菜の卸売価格アラート(10日周期、MAFF旬別結果CSV)、食肉の月次PDF価格取得、FL比率・FLR比率、メニュー収益性ランキング

## デザイン参考実装(design-reference/)

- `design-reference/` に、Figma Makeで作成したUI/UXの参考実装(コード一式)を配置している
- **これは見た目(配色・レイアウト・コンポーネントデザイン)のみの参考であり、データ取得・状態管理・API連携等のロジックは一切含まれていない前提で扱うこと**
- 既存の実装ロジックを変更する際、この参考実装のコードをロジックの模範として扱わないこと(見た目の模倣にのみ使う)
- 今後の画面追加・改修でも、配色・タイポグラフィ・カードや表のデザインパターンは`design-reference/`のトーンに合わせることを基本とする

## コードを読まずに検証する運用(重要)

- 価格計算・FL比率などのコアロジックを変更する際は、**先にテストケースを書かせ、期待値をMahiro自身が手計算/Excelで検算してから**実装に進める
- 実在メニューのサンプルデータを固定の「検算セット」として保持し、機能追加のたびに出力結果が意図せず変化していないか目視確認する
- 一発実装がダメでも気にせず作り直してよい(スロットマシン方式)。ただしrevert判断の基準は必ず「テストが通るか」「検算結果が合っているか」に統一する。コードの見た目の綺麗さでは判断しない

## 自律実行ワークフロー(委任を増やすための運用ルール)

Mahiroはコードを読んで判断できないため、「読まずに安全に任せる」仕組みを徹底すること。

### チェックポイント運用

- 自律的な実装作業を始める前に、必ずgitでコミットしチェックポイントを作ってから進める
- 実装後「セルフチェックループ」がFAILした場合、または検算結果が合わない場合は、部分的な手直しを試みずチェックポイントにrevertしてやり直す(スロットマシン方式)。コードの見た目の綺麗さでrevert判断をしない

### セルフチェックループ(Claude Code自身にレビューさせる)

- 変更のたびにbuild/lint/既存テスト(34本)を自動実行し、結果をPASS/FAILと一言(日本語)で報告すること
- 「実装しました」だけの報告は禁止。必ず検証結果とセットで報告する
- 上記「コードを読まずに検証する運用」の検算セットも、可能な限りこのループに組み込む

### 定型スラッシュコマンド

- /verify — build・lint・テストを実行し、結果を日本語でPASS/FAIL報告
- /diff-summary — 今回の変更内容と理由を平易な日本語で要約
- /session-end — 下記のセッション終了ルーチンを実行

### 非同期委任(GitHub Issue経由)

- まとまった時間が取れない時は、GitHub Issueに日本語でやってほしいことを書いて起票する運用も使う。Claude CodeのGitHub連携で、Mahiroがセッションを開かなくても実装案が提案される。レビューは後でまとめて行う

## ハマりどころ・運用上の注意点

- **マイグレーションファイルの作成と、本番Supabaseへの反映は別ステップ。** `supabase/migrations/`にSQLファイルを追加しただけではDBには反映されない。新しいカラム・cost_type・テーブルを追加する変更をした回は、「マイグレーションファイルを作った」で終わらせず、Mahiro自身がSupabaseのSQL Editor(または`supabase db push`)で実行したことを確認してから、その回の実装を完了扱いにすること

- **デプロイは手動**(`npx vercel --prod`)。`git push` だけでは本番に反映されない。また、この操作は**gitに入っていない(未追跡の)ファイルもアップロードする**ため、`public/`などに置いた作業用ファイルが本番で公開されてしまう。デプロイ前に `git status` で未追跡ファイルを確認すること
- **納品書OCRの費用管理**: β期間中は1ユーザー成功3回・試行10回の上限(`src/lib/invoiceOcr/usageLimit.ts`、DBの関数 `begin_ocr_attempt` / `complete_ocr_success`)。上限の仕組みを外す・緩める変更は、Mahiroの了承を得てから
- 営業用スクリーンショットは、使い捨てのデモアカウントで撮り、撮影後に必ず削除する。共有の市場価格テーブルに偽データを入れない(詳細は HANDOFF.md 5-3)

## 現在のフェーズ・目標(2026年9月時点)

- 1ヶ月の目標は収益化ではなく検証: UI+Supabase接続を完成させ、CrowdWorks経由の実ユーザー3〜5人に無料で使ってもらい、少なくとも1人が「お金を払ってもいい」と思うか確認する
- 無料ユーザーの運用コストはほぼゼロ(CSVマッピングにLLM API不使用、卸売データも無料)。将来的な有償候補は一時金+継続アラート機能のみサブスクというハイブリッド案を検討中(競合レビューで月額サブスク抵抗が見えたため)

## セッション終了時に毎回行うこと

- 今回のセッションで実装した内容と、次回持ち越すべきTODOを日本語で要約する
- このファイルに追記すべき新しい「ハマりどころ」や誤解しやすいルールがあれば提案する
